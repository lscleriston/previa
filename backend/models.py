from pydantic import BaseModel
from typing import Optional, List, Dict

# Opcionalmente reescrevendo/inserindo se quisermos manter tipagem p/ a API, mas como n pediram pra editar isso agr, daremos skip no models e focar no backend de banco...
